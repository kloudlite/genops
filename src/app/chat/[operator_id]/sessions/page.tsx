import { CreateSessionForm } from "./client-side-components";

export default function Page(){
  return <div className="h-full flex justify-center items-center w-full">
    <CreateSessionForm />
  </div>
}